# TODO — active task list for this project.
#
# Single source of truth for active work (see CLAUDE.md -> documentation map).
# Edit through the todo skill so ids, ordering, and metadata stay consistent:
#   scripts/todo.sh <command>      (see .claude/skills/todo/SKILL.md)
# Hand-editing works too — it is plain TOML — but the skill keeps it tidy.

schema_version = 1

[meta]
title = "TODO"
kind = "todo"
purpose = "The live task list and journal for this project — active work only."
documentation_map = "CLAUDE.md"
archive = "FINISHED.toml"

[meta.rules]
add_proposed_work = "Add proposed work as a new task rather than losing it."
remove_dropped_items = "Remove dropped items with a one-line why."
finish_work = "When a task is fully finished (built, tested, documented), run `scripts/todo.sh done <id>` to move it to FINISHED.toml."
scope = "[Project scope rule — what is in and out of bounds for this project.]"

[[task]]
id = "encrypted-full-config-backup-including-private-keys"
title = "Encrypted full-config backup including private keys"
description = "Export the entire app configuration (keys, identities, hosts, connection settings) into a single encrypted backup file that includes private key material. Encryption with a user-supplied passphrase; restore path must import everything back."
status = "active"
category = "feature"
urgency = "normal"
order = 20
created = 2026-08-03
tags = ["backup", "export", "security"]

[[task]]
id = "unencrypted-config-only-backup-no-private-keys"
title = "Unencrypted config-only backup (no private keys)"
description = "Export all configuration details (identities, hosts, ports, options, public keys) as a plaintext/unencrypted file that deliberately excludes private key material, for sharing or version control. Restore should prompt for the missing keys."
status = "active"
category = "feature"
urgency = "normal"
order = 30
created = 2026-08-03
tags = ["backup", "export"]
